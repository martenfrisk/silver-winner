node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Agent worktrees
/.worktrees

# Playwright
/test-results
/playwright-report

# Subagent worktrees (transient, each is its own git repo)
.claude/worktrees/

# Speech corpora for scripts/mine-corpus.ts (own licences, not ours to ship)
/corpus

# Raw Anki export for scripts/match-deck-audio.ts — the user's own deck,
# re-exportable from Anki any time; static/audio-human/ + human-audio-
# manifest.json are the durable output the app actually ships.
/Burmese__BScriptIntro.txt
